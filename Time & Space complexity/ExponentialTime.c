#include <stdio.h>

int main()
{
    // Exponential Time -> O(2^n)

    // int fib(int n) {
    //     if (n <= 1)
    //         return n;
    //     return fib(n - 1) + fib(n - 2);
    // }

    return 0;
}